---
name: obsidian-anki-cards
description: 把学习域里的单元学习笔记提炼成 flashcards-obsidian 插件格式的 Anki 卡片，追加写入该域自己的中间文件（如《XX学习域-Anki卡片.md》），供用户在 Obsidian 里手动同步进 Anki。当用户提到"做闪卡""生成Anki卡片""做卡片""同步到Anki""帮我把这个单元/这几个单元/还没做过的单元做成卡片""flashcards-obsidian"，或者刚学完/复盘完某个学习域的一个单元、想把内容变成可以刷的记忆卡片时，主动使用本 skill，不用等用户指明是哪个域或把插件语法说清楚。用户要求修改、修复、更新、完善中间文件里任何已有卡片时也必须先进入本 skill——哪怕只改一张卡的措辞，不了解插件语法直接编辑会把卡片改碎。不要用于：把笔记逐字照抄成卡片、生成无需记忆的普通笔记、或用户只想整体整理笔记而不走 Anki 的场景（正常聊天即可，无需本 skill）。
---

# 学习域 Anki 闪卡生成

## 这个 skill 解决什么问题

很多学习者光靠"理解"记不住，需要 Anki 的间隔重复把学过的内容真正变成能随口说出来的东西。知识库里常有多个采用"看书→发散→收敛"三段式的学习域（如哲学观念、产品方法论……未来还会有新的），它们的单元笔记结构、判断标准、卡片规范几乎完全一致。做卡片这件事本身不难，难的是每次都要重新想"flashcards-obsidian 这插件语法是什么来着"，以及每新开一个学习域就要重新固化一遍同一套流程——这个 skill 把语法参考和提炼方法固化成**跨域通用**的一套，只在开头做一次"这次是哪个域"的定位，不用为每个学习域各建一个 skill。

**这个 skill 只负责生成卡片文本、写入中间文件。真正同步到 Anki 那一步（`Ctrl+P → Flashcards: generate for the current file`）必须由用户在 Obsidian 里手动执行——skill 不直接操作 Anki，也不该替用户按下这个键。**

## 语法参考

写卡片前先读 `references/flashcards-obsidian-语法.md`，里面是插件的完整语法表（标签式 `#card`/`#card-reverse`/`#card-spaced`、行内 `::`/`:::`、cloze 挖空、frontmatter 里的卡组与标签、context-aware 模式、ID 追踪机制）。**照抄语法，不要凭印象改写格式**——多一个空格、少一个空行，插件就识别不出来。

**本节的适用范围不只是生成新卡：对中间文件的任何编辑（新增、修改、删除卡片，调整排版）都必须先读完本节和语法参考再动手。** 本 skill 的开发过程中踩过真实教训：没读语法就直接改一张已有卡片，在 `#card` 答案里用空行分段，用户同步后卡片碎成几块、长出孤儿 ID，修复成本远高于先花一分钟读语法。

**头号坑（上面教训的技术根源）：`#card` 的答案是从标签下一行开始的连续块，到第一个空行就截止。** 答案内部绝不能用空行分段——空行之后的内容会被甩出卡片、变成不属于任何卡片的散文本；下次同步时插件把前半截当成"没有 ID 的新卡"再插一个新 `^id`，原来的 `^id` 孤儿化留在散文本末尾，一张卡就此裂开。答案需要分层时，用列表项（`- `）承担分段职责，整块行行相连，已有的 ID 行必须紧贴答案最后一行。

**重要坑（查过插件源码 `getContext()` 验证过）：卡片问题绝对不能用 `#`/`##`/`###` markdown 标题写法，一律用纯段落文字 + `#card` 标签。** 原因：`###` 在 Obsidian 里是真实的三级标题，不是视觉样式；插件的 context-aware 逻辑只认标题结构，一张用 `###` 写的卡片会变成"标题"，紧跟在它后面、且中间没有再出现新标题的所有卡片（不管是 `::` 行内卡、cloze 卡还是普通 `#card`），都会把这张"标题卡"的问题误认成自己的祖先标题，一起拼进 Anki 正面，导致正面越叠越长、层级混乱。`##`/`###` 只能用在真正的结构分区上（本文件里就是 `## 单元N · 标题` 这一级），单张卡片的问题永远不加任何 `#` 前缀。

**同一个坑还有一个变体**：文件最顶部如果写了 `# 标题` 这样的一级标题（比如给整份文件起个名），插件也会把它拼进**每一张卡**的正面最前面（`标题 > 单元N · 标题 > 问题`），因为它是从最顶层标题开始拼路径的。中间文件的卡组名已经由 frontmatter 的 `cards-deck` 决定，跟这行 H1 无关，所以**文件顶部的标题不要用 `# ` markdown 语法**，写成加粗文字（如 `**XX学习域**`）或普通文字即可，这样正面就只会是"单元N · 标题 > 问题"，不会多出一层文件标题。

## 工作流程

### 第零步：确定要处理哪个学习域

本 skill 服务的不是单一目录，而是所有遵循"单元学习笔记"模式的学习域（示例域：`13-哲学启蒙/`、`15-产品方法论/`，以后可能有更多）。这些域的共同特征：目录下有自己的档案文件（如 `XX学习域档案.md`）、`AGENTS.md` 里有"单元学习笔记"规范和"Anki 卡片管理"小节、单元笔记统一命名为 `YYYY年M月-单元N-标题.md`。

判断本次处理哪个域，按优先级：

1. **用户明确提到**：域名（如"XX学习域"）或某单元笔记路径——直接定位到对应目录
2. **当前工作目录已经在某个域目录下**——用这个域，不用多问
3. **都判断不出来**：不要凭猜测选一个，直接问用户是哪个域

定位到域目录后，确认三件事（后续步骤里的路径、判断标准、卡组名全部来自这里，不再硬编码）：

- **中间文件名**：优先读该域 `AGENTS.md` 的 `目录结构` 表里 `XXX-Anki卡片.md` 那一行；没有专门约定时按命名惯例用 `{域名}-Anki卡片.md`
- **判断标准原句**：优先用该域 `AGENTS.md`（`文件规范 → 单元学习笔记 → 详略原则`）里写的判断标准；没有时就按内置默认："这段话删掉后，三个月后的我还能想起这一课吗？"
- **卡组名**：读中间文件 frontmatter 的 `cards-deck` 字段（不用去猜，文件里已经写好了）

下面步骤里出现的"域目录""中间文件"，都指第零步定位出来的这两个具体路径。

### 第一步：确定要处理哪些单元

用户可能明确指定单元（"帮我把单元5做成卡片"），也可能笼统地说"把还没做的都做一遍"。后一种情况下，判断"哪些单元已经做过"的方法：

1. 读取该域的中间文件，提取里面所有 `## 单元N · 标题` 这样的二级标题
2. 对照该域目录下所有 `YYYY年M月-单元N-标题.md` 文件，凡是标题已经出现在中间文件里的单元，视为"已做过"，跳过——除非用户明确要求给某个已做过的单元补卡或重做
3. 剩下的就是本次要处理的单元

### 第二步：读取单元笔记，提炼知识点

读取单元笔记全文（不要只扫标题和加粗字），从里面挑值得做成卡片的内容。**提炼的判断标准用第零步确认到的那句"判断标准原句"**（内置默认为：这段话删掉后，三个月后的我还能想起这一课吗？）。能想起来的才做卡片，不是笔记里的每一段话都要卡片化。

优先提炼这几类内容（不必每类都有，视单元实际内容而定，通常一个单元出 4~8 张卡比较合适，宁可少而精，不要为了凑数硬拆；具体措辞随该域的主题调整，比如哲学类域提炼核心命题，方法论类域提炼决策框架）：

- **核心命题**：这一单元最核心的那个翻转性论断或模型是什么（笔记里"核心命题"一类小节通常就是）
- **关键比喻/论证链**：帮助理解命题的那个类比或几层论证（比如用"根与枝叶"解释某个抽象概念成立的关系，用效用/边际这类工具的推导链讲清一个决策模型）
- **关键原文金句**：值得记住准确措辞的那句话，适合做 cloze 挖空，而不是转述大意
- **个人追问与领悟**：笔记里"我的追问""个人领悟""待深挖"里那些真正让用户想通了的转折点，尤其是结合具体经历/场景的复盘——这部分往往比原典本身更值得做成卡片，因为这是他自己消化过的话，更容易唤醒记忆
- **对照表精华**：单元里"对照""小结"表格中特别精炼、值得直接记住的对应关系

**不要做的事**：不要把大段原文整段抄进卡片背面当作"提炼"；不要为了覆盖率把笔记里每个小标题都拆一张卡，那样卡片会又多又薄，复习负担大、记忆效果差。

### 第二步半：写问法（最容易翻车的一步，写完每张卡都要按本节回看一遍）

答案提炼得好、问法写废了，卡片一样是死卡。**唯一的判断标准：半年后只看这张卡的正面（记住正面还会自动带上"卡组 > 单元N · 标题"这层上下文），我能不能想起要答什么？** 半年后学习者早忘了这一课叫什么名字、笔记里怎么写的，正面必须自带足够的抓手。

**必须给的抓手——好问法都带"场景钩子"**：用当时那件具体的事、那个具体的情境起头，而不是用抽象概念名起头。人的记忆是被场景唤醒的，不是被术语唤醒的。

- ❌ `"某概念的核心命题"这一课最大的收获是什么？` → ✅ `遇到一件不知道怎么做才能成的事，这个框架帮不帮得上？它的主场在哪、只管哪两头？`
- ❌ `三思法在这一课的工作焦虑里怎么具体应用？` → ✅ `工作焦虑（心里没底、怕自己不行）时，某个决策法的急所、大场、要害各是什么？`
- ❌ `定式提炼四步法是什么？` → ✅ `把自己做成过的一件事，提炼成能给别人用的定式，哪四步？`

**四条硬禁忌，逐条查**：

1. **禁指代**：`这一课`、`这个领悟`、`这件事`、`上面说的`——这些词在笔记里有指向，脱离笔记就是零信息。一律换成那件事本身的具体描述。
2. **禁同义反复 / 禁题面泄答案**：注意正面会自动拼上单元标题，所以`单元1 · 核心命题 > 某思想家的"核心命题"是什么？`等于把答案抄在了题面上，不用想就"答对"了，复习价值为零。同理，`A 和 B，哪个是地图、哪个是指南针？`把两个答案都摆进了选项里。**问题里出现的词，不能是答案的关键词。**
3. **禁逆向提问**：不要拿答案里的生僻术语当唯一线索反着问（`某位思想家用"XX"三个字回应的是什么难题？`）——记不住术语，整张卡就废了。正着问：先给难题的场景，再问他怎么答。同理，本域自造的简称（比如某学习域里约好的内部黑话）不要直接当题面主语，先把它对应的真实处境写出来。
4. **禁开放式问法**：`最大的收获是什么`、`说明什么`、`怎么用`这类没有唯一答案的问法，复习时无从判断自己答对没有。改成有确定落点的问法——问几步、问哪两头、问各是什么角色、问当事人怎么回应。

**高危句式黑名单（写完/自检时按这几个串机械 grep 一遍，命中的逐张过一遍上面四条禁忌）**：

`什么是X` / `X是什么意思` / `X说明什么` / `X怎么用` / `X指什么` / `X这个类比说明什么` / `这一课` / `这个领悟` / 以`那`开头的问题

这批句式本身不是错的——原典术语、原典金句用"是什么意思"来问，是正当的定义卡，该保留。**真正要揪的是两种**：①主语是本域自造的简称（内部黑话式的词，术语名本身就把答案说完了），等于题面泄答案 + 忘了简称整张卡就废；②题面直接复述了答案的核心表述（`某个"××管不了，××是我的"式的类比说明什么？`）。这两种一律改成"先给真实处境、再问怎么办"。

⚠️ **判据是后写的，卡是先做的**——每次给 skill 补了新判据，必须拿新判据把该域中间文件**从头到尾重扫一遍**，不能只用它检查本次新增的卡。这一条踩过：一条 `什么是"某内部黑话"？` 的卡在第一轮全文自检时被当成"定义题、可接受"放过了，直到判据成文才暴露。

**一条容错设计**：允许在问题里给"结构提示"（`三步是什么`、`哪四步`、`各扮演什么角色`），这是提示答案的**形状**、不是提示答案的**内容**，能显著降低回想门槛而不损失复习价值——和禁忌 2 的区别在于：形状提示不告诉你答案是什么，内容泄露直接给出答案。

**另外**：同一个工具/概念在不同单元各有一张卡时（比如某个决策框架在单元2和单元5各一张），两张卡的问法必须把场景明确区分开，否则复习时互相干扰、两张都记不牢。

### 第三步：给每个知识点选卡片格式

不是随便选，按内容性质对应：

| 内容性质 | 用什么格式 | 为什么 |
|---------|-----------|--------|
| 核心命题、需要一段结构化解释的问题 | 标签式 `#card`（**纯段落文字**打标签，不加 `#`/`##`/`###`） | 答案往往要跨行、带层次，行内格式装不下 |
| 一句话能问完答完的简单事实/定义 | 行内 `问题::答案` | 最省地方，读起来也顺 |
| 术语和含义需要双向都能考自己（看词想含义、看含义想词） | `#card-reverse`（同样是纯段落文字） | 一次定义生成两张卡，覆盖两个方向的回忆路径 |
| 原文金句，想记住准确措辞而非大意 | cloze `==挖空==` | 挖空强迫回忆精确用词，转述大意起不到这个作用 |

**再强调一次**：表格里说的"标签式"是指"问题文字 + `#card`"这个写法本身，不是说要给问题加 markdown 标题符号。四种格式全部写成纯段落，一个 `#`/`##`/`###` 都不加——原因见上面"语法参考"里的坑。

**cloze 挖空绝不能跟行内 `::`/`:::` 写在同一行**：`::` 是行内问答卡的分隔符，cloze 是完全独立的卡片机制，两者混用会让插件在同一行里同时生成两张互相打架的卡（症状是 Anki 里该行对应出现两个 `^id` 标记）。想挖空原文金句，就把整行写成不带 `::` 的纯 cloze 段落（问题背景直接写成段落前半句的说明文字，跟原文连在一起，不用 `::` 分隔）；挖空只用 `==词==` 一种写法，不要写成 `{{c1::词}}`——那是 Anki 内部格式，不是插件语法。

### 排版原则：答案要为手机复习优化

`#card` 标签式的答案支持多行（从标签下一行开始，一直到空行才算卡片结束），这个空间要用来让答案好扫读、好记忆，而不是塞成一整段密文。以下几类内容**必须**拆成多行，不是"可以拆"：

- **并列点**（两个及以上要点）：拆成分点列表，每点前用 `- **关键词**：` 起头，关键词加粗
- **对比/二选一**（A 和 B 两条路、A 与 B 的区别、正反对比）：同样拆行，每一方各占一个列表项，`- **A**：……` / `- **B**：……`，不要写成"A是……，B是……"挤在一句话里
- **步骤类内容**（三步法、四步法之类）：用 `**①xxx**`/`**②xxx**` 这样带序号加粗的短语开头，每步一行，不要把序号和解释挤在同一段里
- **分支判断/条件结果**（如果……就……；情况A对应结果X，情况B对应结果Y）：用 `- 条件 → 结果` 的箭头格式，每个分支单独一行
- **递进链条**（A引出B、B引出C这种概念展开）：可以用 `- 起点 → 落点` 的箭头列表呈现每一环，比如概念的层层展开：`A→B`、`B→C`……这种

其余排版细节：

- **原文引用、核心判断句**在分点列表里也可以用 `**加粗**` 标出来，形成"扫一眼关键词就能唤醒整段记忆"的锚点
- 列表最后如果有一句总结/点睛之笔，直接作为列表的最后一项（前面同样不留空行），不要另起一段——空行会提前截断卡片
- **行内 `::` 格式本身不支持换行**（见语法参考的"限制"一条）。如果一个问题的答案天生就有 2 个以上并列项/编号步骤，**不要硬塞进一行 `::`**，直接改用标签式 `#card` + 上面的列表格式——这是最容易被忽略的一类，选格式时要先看内容是否有并列结构，再决定用 `::` 还是 `#card`，不是先图省事选 `::` 再将就
- **不要为了排版而排版**：一句话能问完答完、内容本身没有并列/对比/步骤结构的简单事实，就用行内 `::`，不必强行拆成列表
- 具体例子见该域中间文件里已有的卡片（好的范例区块通常是"××三步法""××四步骤""某位思想家说改变一个人只有哪两条路"这种颗粒度），照着这个格式写；新域没有范例时，参照这个颗粒度自己拿捏

### 第四步：写入中间文件

目标文件：第零步定位出来的该域中间文件（**已存在，只追加，绝不覆盖**）。

- 每个单元一个 `## 单元N · 标题` 区块，标题措辞跟单元笔记文件名保持一致，方便第一步做去重比对
- 新区块直接追加到文件末尾即可，不用费劲按单元序号插入到中间——Anki 卡片本身没有顺序要求，插入到中间反而容易搞乱已有内容
- 卡片全部写在对应的 `## 单元N · 标题` 这个二级标题下面（呼应语法参考里的 context-aware 说明），这样每张卡片的 Anki 正面才会自动带上"{卡组名} > 单元N · 标题"这层上下文（卡组名即第零步读到的 `cards-deck`）；但卡片自己的问题**绝不能**再用 `#`/`##`/`###` 写成标题，一律纯段落文字，理由见前面"重要坑"一条
- 如果该域的中间文件里已经有人工写的范例区块（四种格式都有，颗粒度和语气可以直接照着学）；如果是新域、中间文件还没有范例，就按上面"排版原则"和格式对应表自己拿捏颗粒度，宁可少而精
- **头部警示检查**：中间文件 frontmatter 之后必须有一段"修改前必读"警示（模板见下），提醒后来者（人或 AI）动这个文件前先读本 skill。新建域的中间文件时照模板写入；处理已有文件时顺手检查这段警示在不在，缺了就补上，不用请示。**注意：头部警示只保留"修改前必读"和"用法"两段简短提示，不要在生成的文件里罗列语法坑点——语法细节由 skill 本身和语法参考文件承担，写到生成文件里反而因为特殊符号导致格式出错。**

中间文件头部模板（frontmatter 之后，域名用加粗文字、不用 `#` 标题，原因见"语法参考"里的 H1 坑）：

```markdown
**{域名}**

> **⚠️ 修改前必读**：对本文件做任何改动（新增/修改/删除卡片）之前，必须先读 `obsidian-anki-cards` skill 及其语法参考，不懂语法直接改会把卡片改碎。
>
> **用法**：这是给 flashcards-obsidian 用的卡片草稿本。改完后在 Obsidian 里对本文件跑 `Ctrl+P → Flashcards: generate for the current file`（Anki 的 Browse 窗口要关着），卡片同步进 Anki 的「{卡组名}」卡组；首次同步后插件自动给每张卡插入 ID 行，别删；想加新卡，照已有格式续写、不写 ID 行。
```

### 第五步：全文自检与优化（每次都要做，不限于本次新增的卡）

写完新卡后，**回头通读整份中间文件**（不是只看本次新加的区块），逐张卡对照上面的"排版原则"检查一遍：

- **有没有 `#card` 答案内部夹着空行的？** 空行会把卡片腰斩：空行后的内容不属于任何卡片，同步后前半截长出新 ID、原 ID 孤儿化。发现了把答案改回无空行的连续列表块
- 有没有 2 个以上并列点/对比项/步骤挤在一整段里，没有拆成列表的？
- 有没有 `::` 行内答案里其实藏着并列项或编号步骤，被迫塞成一行、读起来费劲的？（发现了就改成 `#card` 标签式 + 列表）
- 有没有分支判断（如果……就……）写成大段叙述，没用 `→` 箭头分行的？
- **有没有 `::`/`:::` 行内分隔符跟 `==...==`/`{...}` cloze 挖空写在同一行的**（比如 `问题::「原文……==挖空词==……」`）？这是两套卡片机制冲突，会在同一行生成两张互相打架的卡，Anki 里的症状是这一行对应出现两个 `^id` 标记。发现了要二选一：想留 cloze 效果就去掉 `::`，把整行改写成不带分隔符的纯 cloze 段落；想留行内问答就去掉 `==...==`，答案改成普通文字
- **有没有卡片问题被写成了 `#`/`##`/`###` 标题**（比如 `### 问题 #card`）？这会让紧跟在它后面、没有再打新标题的卡片把它误认成祖先标题，一起拼进 Anki 正面。发现了直接去掉行首的 `#` 号，改成纯段落文字

**问法检查（按第二步半的四条禁忌逐张过，这是最容易攒下技术债的一类）**：

- 有没有卡片问题里出现 `这一课`/`这个领悟`/`这件事` 这类**指代**，脱离笔记就没有指向的？
- 有没有问题跟它所在的单元标题**同义反复**，或者把答案的关键词直接摆进了题面（尤其"A 和 B，哪个是 X、哪个是 Y"这种把两个答案都列出来的问法）？
- 有没有拿答案里的**生僻术语或本域自造简称**当题面主语反着问（`XX 三步法是什么`、`XX 之喻说明什么`），记不住那个词整张卡就废掉的？
- 有没有 `最大的收获是什么`/`说明什么`/`怎么用` 这类**开放式问法**，复习时无法判断自己答对没有的？
- 同一个工具/概念在**多个单元各有一张卡**时，问法有没有把场景区分开？

**答案负荷检查**：有没有一张卡的答案跨了 3 个以上不同层次（比如"破什么 + 立三条 + 日常怎么做"塞在一张卡里）？这种卡复习时必然答不全，要拆成两张。**拆卡的正确做法**：原 `^id` 留给拆出来的其中一张（内容上跟原卡最接近的那张），另一张**不写 ID 行**，等插件同步时自动生成；两张卡之间必须留一个空行。

**`#card-reverse` 误用检查**：反转卡会拿整块答案当正面、问"这是什么"，所以只有"答案首句就是一个明确的单一术语定义"（如 `**某个术语**=……`）才配得上 reverse。如果答案是两个并列概念（`两个并列概念各指什么`）或含"对自身的诊断"这类附加段落，反向那张根本答不出——降级成普通 `#card`。

发现不达标的卡片，**直接原地改好，不需要跟用户确认**——只改标记（`^id`）上方的文字，`^id` 那一行一个字都不能动，这样插件同步时认得出是同一张卡，做的是更新而不是重复新建。这一步不局限于本次新增的单元，只要通读时发现全文任何位置有不达标的旧卡，都顺手一起优化，让整份文件风格统一。

**例外**：如果发现某张卡片下面连续出现两行 `^id` 标记（比如 `^123` 紧跟着 `^456`），这是同步异常留下的痕迹，不要自己删除或合并——直接告诉用户，让用户去 Anki 的 Browse 窗口核对这两个 ID 分别对应哪张卡，确认后再处理。

**绝对不能凭空编造 `^id`（哪怕看起来是"接着已有编号往后排"）**：`^id` 只能由 flashcards-obsidian 插件在真实同步后自动写入，它对应的是 Anki 里一张真实存在的笔记。凭空写一个 `^178xxxxxxxxxx` 这样的数字上去，插件下次同步时会把它当成"这张卡已经同步过、Anki 里有对应笔记"，于是尝试按这个 ID 去**更新**一张根本不存在的笔记——直接报错或产生数据错乱。规则就两条，不删不生成：
- **不删**：已有卡片的 `^id` 行，一个字都不能动，哪怕内容改了也只改标记上方的文字
- **不生成**：新写的卡片，无论标签式、行内、cloze，一律不写 `^id` 行——写完卡片文字就此打住，那一行留给插件自己在真实同步后补上
- 如果哪天真的需要某张卡拿到一个新 ID（比如想让插件重新识别成一张新卡），正确做法是**先把这张卡的问答内容删空，只留 `^id` 单独一行**，跑一次同步让插件在 Anki 里把对应笔记删掉，再删除这行 `^id`、换上新内容，重新同步——插件会把它当全新卡处理并写入真正的新 ID。这个操作前必须先跟用户确认，不要自作主张执行。

### 第六步：收尾，提醒用户手动同步

写完后用一两句话告诉用户：写了几张新卡、覆盖了哪些单元、大致是什么格式分布；如果第五步顺手优化了旧卡，也说一句优化了几张、大致是什么问题（比如"把3张挤成一段的对比卡拆成了列表"）。然后提醒：去 Obsidian 打开这个文件，`Ctrl+P → Flashcards: generate for the current file` 才会真正同步到 Anki；如果 Anki 的 Browse 窗口开着，先关掉再跑这个命令。

**不要自己去调用任何 Anki 相关的命令行工具或 API 尝试"帮用户同步"**——这一步的手感和确认权应该留给用户自己在 Obsidian 里完成。
