---
name: obsidian-anki-cards
description: 把学习域里的单元学习笔记提炼成 flashcards-obsidian 插件格式的 Anki 卡片，追加写入该域自己的中间文件（如《XX学习域-Anki卡片.md》），供用户在 Obsidian 里手动同步进 Anki。当用户提到"做闪卡""生成Anki卡片""做卡片""同步到Anki""帮我把这个单元/这几个单元/还没做过的单元做成卡片""flashcards-obsidian"，或者刚学完/复盘完某个学习域的一个单元、想把内容变成可以刷的记忆卡片时，主动使用本 skill，不用等用户指明是哪个域或把插件语法说清楚。用户要求修改、修复、更新、完善中间文件里任何已有卡片时也必须先进入本 skill——哪怕只改一张卡的措辞，不了解插件语法直接编辑会把卡片改碎。不要用于：把笔记逐字照抄成卡片、生成无需记忆的普通笔记、或用户只想整体整理笔记而不走 Anki 的场景（正常聊天即可，无需本 skill）。
license: MIT
---

# 学习域 Anki 闪卡生成

## 这个 skill 解决什么问题

很多学习者光靠"理解"记不住，需要 Anki 的间隔重复把学过的内容变成能随口说出来的东西。难的不是做卡片本身，是每次都要重新想"flashcards-obsidian 这插件语法是什么来着"，以及每新开一个学习域（经济学入门、设计模式……）就要重新固化一遍同一套流程——这些域的笔记结构、判断标准、卡片规范其实几乎完全一致。本 skill 把语法参考和提炼方法固化成**跨域通用**的一套，只在开头做一次"这次是哪个域"的定位，不用为每个域各建一个 skill。

**这个 skill 只负责生成卡片文本、写入中间文件。真正同步到 Anki 那一步（`Ctrl+P → Flashcards: generate for the current file`）必须由用户在 Obsidian 里手动执行——skill 不直接操作 Anki，也不该替用户按下这个键。**

> 📌 **本文档里的"经济学入门""设计模式""单元3 · 沉没成本"全都只是示例**，用来把规则讲具体。你的域叫什么、目录和文件怎么命名，skill 一概不假设，全部在第零步现场确认。示例里的术语，换成你自己域里的概念即可。

## 语法参考与四条铁律

写卡片前先读 `references/flashcards-obsidian-语法.md`，那是插件的完整语法表（标签式 `#card`/`#card-reverse`/`#card-reminder`、fenced 块 ` ```flashcard `、行内 `::`/`:::`、cloze 挖空、frontmatter 里的卡组与标签、context-aware 模式、ID 追踪机制）。**照抄语法，不要凭印象改写格式**——多一个空格、少一个空行，插件就识别不出来。

**适用范围不只是生成新卡：对中间文件的任何编辑（新增、修改、删除卡片，调整排版）都必须先读完本节和语法参考再动手。** 本 skill 的开发过程中踩过真实教训：没读语法就直接改一张已有卡片，在 `#card` 答案里用空行分段，用户同步后卡片碎成几块、长出孤儿 ID，修复成本远高于先花一分钟读语法。

以下四条是所有坑里最致命的，**后续步骤只按编号引用，不再重复**。每条的完整成因、源码依据和症状，在语法参考对应小节里都有——按规则写就不会踩到，踩到了或想搞清为什么，去那边看。

- **铁律①｜`#card` 的答案取法在 v2 下变了，先判"marker 后同段有没有内容"再写。** 判据（按 v2 源码核对）：`#card` 写在问题行行尾、marker 后面同一段没有别的文字时，v2 取**下一个顶层节点**——紧邻的下一行直接是列表，**整个列表都是答案（合法）**，这是文件里大量卡片的标准写法；但 `问题 #card` 换行后又写了答案文字、再接列表，答案只取同段那句，**列表被甩出**，卡片裂开（v1 时代的"问题 #card + 答案第一行 + 列表"写法在 v2 下失效）。**拿不准时用 fenced 块（` ```flashcard `，见语法参考第 2 节）**，整块行行相连；已有的 ID 行必须紧贴卡片最后一行。
- **铁律②｜卡片问题一律纯段落文字 + 标签，绝不加 `#`/`##`/`###`；文件顶部也不写 `# ` 一级标题**（域名写成加粗文字，如 `**经济学入门**`）。插件的 context-aware 只认真实标题，用标题写法的卡片会被后面的卡片当成祖先路径拼进正面，越叠越长；顶部 H1 则会拼进每一张卡的正面。`#`/`##`/`###` 只留给真正的结构分区（中间文件里就是 `## 单元N · 标题` 这一级）。
- **铁律③｜cloze 挖空有两个坑。** 坑一：挖空绝不能跟行内 `::`/`:::` 写在同一行——两套独立的卡片机制混用，会在同一行生成两张互相打架的卡。想挖空金句就把整行写成不带 `::` 的纯 cloze 段落，问题背景直接作为段落前半句跟原文连在一起。坑二：**一句话想挖多个空，必须用 `{1:词}` 复用编号**——v2 里 `==词==` 每个标记自增编号（第一个 c1、第二个 c2），会各自生成一张独立卡；而且 Anki 不会为新增编号自动补卡（AnkiConnect 更新既有卡只改内容、不重建卡片），症状就是"第二个空没生成"、在卡面上显示成明文。所以**单空用 `==词==`**（Obsidian 里显示好看），**多空一律 `{1:词}` `{1:词}` 绑成一张卡**。`{{cN::词}}` 是 Anki 原生格式，v2 虽然支持但 Obsidian 里显示难看，不主动写。
- **铁律④｜`^id` 不删、不生成、不编造。** 它只能由插件在真实同步后写入，对应 Anki 里一张真实存在的笔记。**不删**：已有 `^id` 行一个字都不能动，改内容只改标记上方的文字（这样同步时是更新，不是重复新建）。**不生成**：新写的卡片一律不写 `^id` 行，留给插件补。凭空编一个（哪怕"接着已有编号往后排"）会让插件去更新一张不存在的笔记，直接报错或数据错乱。

> 需要让某张卡重新拿一个新 ID 时，正确做法是**先把问答内容删空、只留 `^id` 单独一行**，同步一次让插件在 Anki 里删掉对应笔记，再删这行 `^id`、换上新内容重新同步。**执行前必须先跟用户确认。**

**`#card-spaced` 已废弃（v2 改为 `#card-reminder`）**：语法参考里收录了提醒卡写法，但本 skill 只产出 `#card` / `#card-reverse` / 行内 `::` / cloze / fenced 五种，不要主动使用提醒卡，除非用户点名要。

## 工作流程

### 第零步：确定要处理哪个学习域

本 skill 服务的不是单一目录，而是所有遵循"单元学习笔记"模式的学习域。共同特征只有两条：一个域一个目录；域内有若干单元笔记，能稳定看出单元序号和标题。**目录名、文件名怎么起，skill 不做任何假设**（作者自己的库用 `YYYY年M月-单元N-标题.md`，你的可以完全不同）。域内如果有 `AGENTS.md`/`CLAUDE.md` 写了自己的约定，以那份约定为准。

判断本次处理哪个域，按优先级：

1. **用户明确提到**：域名或某篇单元笔记的路径——直接定位到对应目录
2. **当前工作目录已经在某个域目录下**——用这个域，不用多问
3. **整个库就只有一个学习域**（或用户压根只有一个笔记目录）——用它，不用多问
4. **都判断不出来**：不要凭猜测选一个，直接问用户是哪个域

定位到域目录后，确认三件事（后续步骤里的路径、判断标准、卡组名全部来自这里，不再硬编码）：

- **中间文件名**：优先用该域 `AGENTS.md`/`CLAUDE.md` 里约定的文件名；没有专门约定时按命名惯例用 `{域名}-Anki卡片.md`
- **判断标准原句**：优先用该域约定里写的详略标准；没有时就按内置默认："这段话删掉后，三个月后的我还能想起这一课吗？"
- **卡组名**：读中间文件 frontmatter 的 `cards-deck` 字段（不用去猜，文件里已经写好了）

**中间文件还不存在时（新域冷启动）**：不要静默创建、也不要自己编一个卡组名。先问用户两件事——① Anki 里想同步进哪个卡组（可以是尚不存在的新卡组名，插件会自动建）；② 中间文件放在域目录下、叫什么名字（给出 `{域名}-Anki卡片.md` 作为默认建议）。拿到答复后，按第四步的完整模板新建文件，再继续往下走。

下面步骤里出现的"域目录""中间文件"，都指第零步定位出来的这两个具体路径。

### 第一步：确定要处理哪些单元

用户可能明确指定单元（"帮我把单元5做成卡片"），也可能笼统地说"把还没做的都做一遍"。后一种情况下，判断"哪些单元已经做过"的方法：

1. 读取该域的中间文件，提取里面所有 `## 单元N · 标题` 这样的二级标题
2. 对照该域目录下的单元笔记文件，凡是单元序号/标题已经出现在中间文件里的，视为"已做过"，跳过——除非用户明确要求给某个已做过的单元补卡或重做
3. 剩下的就是本次要处理的单元

**文件名对不上时的兜底**：上面第 2 条默认笔记名里能读出单元序号（作者库里是 `YYYY年M月-单元N-标题.md`）。你的域命名不同没关系——退而用笔记标题跟中间文件里已有的 `## ` 区块标题比对；如果笔记是按主题命名、根本没有单元序号这个概念，就把"我判断出还没做过的笔记"列成清单交给用户确认，**不要猜**。

### 第二步：读取单元笔记，提炼知识点

读取单元笔记全文（不要只扫标题和加粗字），从里面挑值得做成卡片的内容。**提炼的判断标准用第零步确认到的那句"判断标准原句"**（内置默认为：这段话删掉后，三个月后的我还能想起这一课吗？）。能想起来的才做卡片，不是笔记里的每一段话都要卡片化。

优先提炼这几类内容（不必每类都有，视单元实际内容而定，通常一个单元出 4~8 张卡比较合适，宁可少而精，不要为了凑数硬拆；具体措辞随该域的主题调整，比如哲学类域提炼核心命题，方法论类域提炼决策框架）：

- **核心命题**：这一单元最核心的那个翻转性论断或模型是什么（笔记里"核心命题"一类小节通常就是）
- **关键比喻/论证链**：帮助理解命题的那个类比或几层论证（比如用"沉没成本"讲清为什么该止损，用边际效用递减推出"钻石为什么比水贵"）
- **关键原文金句**：值得记住准确措辞的那句话，适合做 cloze 挖空，而不是转述大意
- **个人追问与领悟**：笔记里"我的追问""个人领悟""待深挖"里那些真正让用户想通了的转折点，尤其是结合具体经历/场景的复盘——这部分往往比原典本身更值得做成卡片，因为这是他自己消化过的话，更容易唤醒记忆
- **对照表精华**：单元里"对照""小结"表格中特别精炼、值得直接记住的对应关系

**不要做的事**：不要把大段原文整段抄进卡片背面当作"提炼"；不要为了覆盖率把笔记里每个小标题都拆一张卡，那样卡片会又多又薄，复习负担大、记忆效果差。

### 第二步半：写问法（最容易翻车的一步，写完每张卡都要按本节回看一遍）

答案提炼得好、问法写废了，卡片一样是死卡。**唯一的判断标准：半年后只看这张卡的正面（记住正面还会自动带上"卡组 > 单元N · 标题"这层上下文），我能不能想起要答什么？** 半年后学习者早忘了这一课叫什么名字、笔记里怎么写的，正面必须自带足够的抓手。

**必须给的抓手——好问法都带"场景钩子"**：用当时那件具体的事、那个具体的情境起头，而不是用抽象概念名起头。人的记忆是被场景唤醒的，不是被术语唤醒的。

- ❌ `"沉没成本"这一课最大的收获是什么？` → ✅ `一个项目已经砸进去两年、眼看做不成，要不要接着投？这个决定该只看什么、必须不看什么？`
- ❌ `边际效用递减在这一课的定价问题里怎么用？` → ✅ `第一杯水和第八杯水，你愿意付的钱为什么差这么多？这条规律怎么解释"钻石比水贵"？`
- ❌ `观察者模式是什么？` → ✅ `一个对象变了要通知一堆数量不定的其他对象，又不想让它们互相知道对方存在——用哪个模式？它靠什么解耦？`

三个 ✅ 的共同点：**题面里一个答案关键词都没有，但处境描述得足够具体，半年后光看这句话就知道在问哪件事。**

**四条硬禁忌，逐条查**：

1. **禁指代**：`这一课`、`这个领悟`、`这件事`、`这个框架`、`上面说的`——这些词在笔记里有指向，脱离笔记就是零信息。一律换成那件事本身的具体描述。
2. **禁同义反复 / 禁题面泄答案**：注意正面会自动拼上单元标题，所以 `单元3 · 沉没成本 > 沉没成本的核心命题是什么？` 等于把答案抄在了题面上，不用想就"答对"了，复习价值为零。同理，`机会成本和沉没成本，哪个看未来、哪个看过去？` 把两个答案都摆进了选项里。**问题里出现的词，不能是答案的关键词。**
3. **禁逆向提问**：不要拿答案里的生僻术语当唯一线索反着问（`"帕累托改进"三个字回应的是什么难题？`）——记不住术语，整张卡就废了。正着问：先给难题的场景，再问它怎么答。同理，**本域自造的简称**（你在笔记里给某个领悟起的、只有自己懂的名字，比如 `"两年墙"`、`"第八杯水法则"` 这类）不要直接当题面主语，先把它对应的真实处境写出来。
4. **禁开放式问法**：`最大的收获是什么`、`说明什么`、`怎么用`这类没有唯一答案的问法，复习时无从判断自己答对没有。改成有确定落点的问法——问几步、问哪两头、问各是什么角色、问当事人怎么回应。

**高危句式黑名单（写完/自检时按这几个串机械 grep 一遍，命中的逐张过一遍上面四条禁忌）**：

`什么是X` / `X是什么意思` / `X说明什么` / `X怎么用` / `X指什么` / `X这个类比说明什么` / `这一课` / `这个领悟` / 以`那`开头的问题

这批句式本身不是错的——公认术语、原典金句用"是什么意思"来问，是正当的定义卡，该保留。**真正要揪的是两种**：①主语是本域自造的简称，术语名本身就把答案说完了，等于题面泄答案 + 忘了简称整张卡就废；②题面直接复述了答案的核心表述（如 `"钻石比水贵"这个现象说明什么？`——答案的核心表述已经抄在题面上了）。这两种一律改成"先给真实处境、再问怎么办"。

⚠️ **判据是后写的，卡是先做的**——每次给 skill 补了新判据，必须拿新判据把该域中间文件**从头到尾重扫一遍**，不能只用它检查本次新增的卡。这一条踩过：一条 `什么是"某内部黑话"？` 的卡在第一轮全文自检时被当成"定义题、可接受"放过了，直到判据成文才暴露。

**一条容错设计**：允许在问题里给"结构提示"（`三步是什么`、`哪四步`、`各扮演什么角色`），这是提示答案的**形状**、不是提示答案的**内容**，能显著降低回想门槛而不损失复习价值——和禁忌 2 的区别在于：形状提示不告诉你答案是什么，内容泄露直接给出答案。

**另外**：同一个工具/概念在不同单元各有一张卡时（比如"机会成本"在单元2和单元5各一张），两张卡的问法必须把场景明确区分开，否则复习时互相干扰、两张都记不牢。

### 第三步：给每个知识点选卡片格式

不是随便选，按内容性质对应（四种格式的问题**全部**写成纯段落文字，一个 `#`/`##`/`###` 都不加，见铁律②）：

| 内容性质 | 用什么格式 | 为什么 |
|---------|-----------|--------|
| 核心命题、需要一段结构化解释的问题 | fenced 块或标签式 `#card` | 结构化/复杂多行答案推荐用 fenced 块（` ```flashcard `，`front:`/`back:` 多行）；紧邻纯列表可用行尾 `#card`，单段答案用 `#card` |
| 一句话能问完答完的简单事实/定义 | 行内 `问题::答案` | 最省地方，读起来也顺 |
| 术语和含义需要双向都能考自己（看词想含义、看含义想词） | `#card-reverse` | 一次定义生成两张卡，覆盖两个方向的回忆路径 |
| 原文金句，想记住准确措辞而非大意 | cloze 挖空（不与 `::` 同行，见铁律③） | 挖空强迫回忆精确用词，转述大意起不到这个作用。**单空用 `==挖空==`；一句话想挖多个词，用 `{1:词}` `{1:词}` 复用编号绑成一张卡** |

四种格式的写法范例见 `examples/示例-Anki卡片.md`——那是一份可以直接照抄结构的完整中间文件。

### 排版原则：答案要为手机复习优化

**多行/列表答案推荐用 fenced 块或紧邻纯列表的 `#card`**（见铁律①），这个空间要用来让答案好扫读、好记忆，而不是塞成一整段密文。以下几类内容**必须**拆成多行，不是"可以拆"：

- **并列点**（两个及以上要点）：拆成分点列表，每点前用 `- **关键词**：` 起头，关键词加粗
- **对比/二选一**（A 和 B 两条路、A 与 B 的区别、正反对比）：同样拆行，每一方各占一个列表项，`- **A**：……` / `- **B**：……`，不要写成"A是……，B是……"挤在一句话里
- **步骤类内容**（三步法、四步法之类）：用 `**①xxx**`/`**②xxx**` 这样带序号加粗的短语开头，每步一行，不要把序号和解释挤在同一段里
- **分支判断/条件结果**（如果……就……；情况A对应结果X，情况B对应结果Y）：用 `- 条件 → 结果` 的箭头格式，每个分支单独一行
- **递进链条**（A引出B、B引出C这种概念展开）：用 `- 起点 → 落点` 的箭头列表呈现每一环

其余排版细节：

- **原文引用、核心判断句**在分点列表里也可以用 `**加粗**` 标出来，形成"扫一眼关键词就能唤醒整段记忆"的锚点
- 列表最后如果有一句总结/点睛之笔，直接作为列表的最后一项，不要另起一段（铁律①）
- **行内 `::` 不支持换行**（见语法参考"限制"一条）。答案天生有 2 个以上并列项/编号步骤的，**不要硬塞进一行 `::`**，改用 fenced 块或 `#card` + 紧邻列表——这是最容易被忽略的一类：选格式时先看内容有没有并列结构，别图省事先选 `::` 再将就
- **不要为了排版而排版**：一句话能问完答完、内容本身没有并列/对比/步骤结构的简单事实，就用行内 `::`，不必强行拆成列表
- 颗粒度拿捏见 `examples/示例-Anki卡片.md`，或该域中间文件里已有的卡片；好的范例通常是"××三步法""××四步骤""A 和 B 的分工各是什么"这种粒度

### 第四步：写入中间文件

目标文件：第零步定位出来的该域中间文件。**已存在就只追加、绝不覆盖；不存在则按第零步的冷启动流程问清卡组名后，用下面的完整模板新建。**

- 每个单元一个 `## 单元N · 标题` 区块，标题措辞跟单元笔记保持一致，方便第一步做去重比对
- 新区块直接追加到文件末尾即可，不用费劲按单元序号插入到中间——Anki 卡片本身没有顺序要求，插入到中间反而容易搞乱已有内容
- 卡片全部写在对应的 `## 单元N · 标题` 这个二级标题下面（呼应语法参考里的 context-aware 说明），这样每张卡片的 Anki 正面才会自动带上"{卡组名} > 单元N · 标题"这层上下文；卡片自己的问题绝不能再写成标题（铁律②）
- 该域中间文件里如果已经有人工写的范例区块，四种格式都有，颗粒度和语气可以直接照着学；如果是新域、文件里还没有范例，就照 `examples/示例-Anki卡片.md` 和上面"排版原则"拿捏颗粒度，宁可少而精
- **头部警示检查**：中间文件 frontmatter 之后必须有一段"修改前必读"警示（模板见下），提醒后来者（人或 AI）动这个文件前先读本 skill。新建文件时照模板写入；处理已有文件时顺手检查这段警示在不在，缺了就补上，不用请示。**注意：头部警示只保留"修改前必读"和"用法"两段简短提示，不要在生成的文件里罗列语法坑点**——语法细节由 skill 本身和语法参考文件承担，写到生成文件里反而因为特殊符号导致格式出错

新建中间文件的完整模板（`{}` 里的内容按第零步确认到的实际值替换；域名用加粗文字、不用 `# ` 标题，见铁律②）：

```markdown
---
cards-deck: {卡组名}
tags: {全局标签，可留空或删掉这一行}
---

**{域名}**

> **⚠️ 修改前必读**：对本文件做任何改动（新增/修改/删除卡片）之前，必须先读 `obsidian-anki-cards` skill 及其语法参考，不懂语法直接改会把卡片改碎。
>
> **用法**：这是给 flashcards-obsidian 用的卡片草稿本。改完后在 Obsidian 里对本文件跑 `Ctrl+P → Flashcards: generate for the current file`（Anki 的 Browse 窗口要关着），卡片同步进 Anki 的「{卡组名}」卡组；首次同步后插件自动给每张卡插入 ID 行，别删；想加新卡，照已有格式续写、不写 ID 行。
```

`cards-deck` 支持用双冒号建子卡组（`{域名}::单元1`），一般不需要——单元层级已经由 `## 单元N · 标题` 拼进卡片正面了。

### 第五步：全文自检与优化（每次都要做，不限于本次新增的卡）

写完新卡后，**回头通读整份中间文件**（不是只看本次新加的区块），逐张卡过一遍下面三组检查。

**格式检查**（对应四条铁律，发现即改）：

- 有没有 `#card` 卡踩了"marker 后同段有内容 + 再接列表"的截断坑？（铁律①）——`问题 #card` 行尾紧邻直接跟列表是**合法**的，别误改；踩坑的才改成 fenced 块，答案整体放进 `back:` 里
- 有没有卡片问题被写成了 `#`/`##`/`###` 标题（比如 `### 问题 #card`），或者文件顶部有 `# ` 一级标题？（铁律②）去掉行首的 `#`，改成纯段落文字 / 加粗文字
- 有没有 `::`/`:::` 跟 `==...==`、`{...}` 写在同一行？（铁律③）二选一：想留 cloze 就去掉 `::` 改写成纯 cloze 段落；想留行内问答就去掉挖空标记
- 有没有**同一行里多个 `==...==`**（多空没复用编号）？（铁律③）`==` 会自增编号各自成卡、且 Anki 不自动补卡，改成 `{1:词}` `{1:词}` 复用编号
- 有没有 2 个以上并列点/对比项/步骤挤在一整段里，没有拆成列表的？
- 有没有 `::` 行内答案里其实藏着并列项或编号步骤，被迫塞成一行、读起来费劲的？（改成 fenced 块或 `#card` 标签式 + 紧邻列表）
- 有没有分支判断（如果……就……）写成大段叙述，没用 `→` 箭头分行的？

**问法检查**（按第二步半的四条禁忌逐张过，这是最容易攒下技术债的一类）：

- 禁忌1：问题里有 `这一课`/`这个领悟`/`这件事`/`这个框架` 这类**指代**，脱离笔记就没有指向的？
- 禁忌2：问题跟它所在的单元标题**同义反复**，或把答案关键词直接摆进了题面（尤其"A 和 B，哪个是 X、哪个是 Y"这种把两个答案都列出来的问法）？
- 禁忌3：拿答案里的**生僻术语或本域自造简称**当题面主语反着问，记不住那个词整张卡就废掉的？
- 禁忌4：`最大的收获是什么`/`说明什么`/`怎么用` 这类**开放式问法**，复习时无法判断自己答对没有的？
- 同一个工具/概念在**多个单元各有一张卡**时，问法有没有把场景区分开？

**内容检查**：

- **答案负荷**：有没有一张卡的答案跨了 3 个以上不同层次（比如"破什么 + 立三条 + 日常怎么做"塞在一张卡里）？这种卡复习时必然答不全，要拆成两张。**拆卡的正确做法**：原 `^id` 留给拆出来的其中一张（内容上跟原卡最接近的那张），另一张**不写 ID 行**、等插件同步时自动生成；两张卡之间必须留一个空行。
- **`#card-reverse` 误用**：反转卡会拿整块答案当正面、问"这是什么"，所以只有"答案首句就是一个明确的单一术语定义"（如 `**某个术语**=……`）才配得上 reverse。如果答案是两个并列概念，或含"对自身的诊断"这类附加段落，反向那张根本答不出——降级成普通 `#card`。

发现不达标的卡片，**直接原地改好，不需要跟用户确认**——但只改 `^id` 上方的文字，`^id` 那一行一个字都不能动（铁律④）。这一步不局限于本次新增的单元，只要通读时发现全文任何位置有不达标的旧卡，都顺手一起优化，让整份文件风格统一。

**例外**：如果发现某张卡片下面连续出现两行 `^id` 标记（比如 `^123` 紧跟着 `^456`），这是同步异常留下的痕迹，不要自己删除或合并——直接告诉用户，让用户去 Anki 的 Browse 窗口核对这两个 ID 分别对应哪张卡，确认后再处理。

### 第六步：收尾，提醒用户手动同步

写完后用一两句话告诉用户：写了几张新卡、覆盖了哪些单元、大致是什么格式分布；如果第五步顺手优化了旧卡，也说一句优化了几张、大致是什么问题（比如"把3张挤成一段的对比卡拆成了列表"）。然后提醒：去 Obsidian 打开这个文件，`Ctrl+P → Flashcards: generate for the current file` 才会真正同步到 Anki；如果 Anki 的 Browse 窗口开着，先关掉再跑这个命令。

**不要自己去调用任何 Anki 相关的命令行工具或 API 尝试"帮用户同步"**——这一步的手感和确认权应该留给用户自己在 Obsidian 里完成。
